# Application name
spring.application.name=pdfapp

# Oracle Database connection
spring.datasource.url=jdbc:oracle:thin:@localhost:1521/XE
spring.datasource.username=SYSTEM
spring.datasource.password=Sravani9756
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.Oracle12cDialect

# Server port
server.port=8080

# Static resources for PDF download
spring.mvc.static-path-pattern=/uploaded_pdfs/**
spring.web.resources.static-locations=file:uploaded_pdfs/

# Optional: Allow bean overriding (if you ever get conflicts like passwordEncoder)
spring.main.allow-bean-definition-overriding=true
